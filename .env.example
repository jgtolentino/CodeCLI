# Codex Environment Variables
# Copy this file to .env and update values

# OpenAI API Key (REQUIRED for code generation and API testing)
# Get your API key from https://platform.openai.com/api-keys
OPENAI_API_KEY=your_openai_api_key_here

# OpenAI Model Selection (Optional)
# Default model is gpt-4o, but you can change it to:
# - gpt-4o-mini (faster but less capable)
# - gpt-4 (older version)
# - gpt-3.5-turbo (much faster but less capable)
OPENAI_MODEL=gpt-4o

# Temperature Setting (0.0 to 1.0) (Optional)
# Lower values make responses more deterministic
# Higher values make responses more creative
OPENAI_TEMPERATURE=0.7

# Server Configuration (Optional)
PORT=1227

# Template Configuration (Optional)
TEMPLATE_DIR=template
DATA_DIR=data
OUTPUT_DIR=out

# Enable debug output (true/false) (Optional)
DEBUG=false

# Code Output Directory (Optional)
# Where generated code files will be saved
CODE_OUTPUT_DIR=output/code