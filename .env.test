# Test Environment Variables for Docker
# Do not add real credentials to this file

# OpenAI API Key (for testing only - use an invalid format key)
OPENAI_API_KEY=sk-test-not-a-real-key-replace-with-your-actual-key

# Server Configuration
PORT=8080

# Docker Configuration
MCP_SERVER=http://mcp:3000